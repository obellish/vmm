#[doc(inline)]
pub use {
	vmm_alloc_stats as alloc_stats, vmm_interpret as interpret, vmm_ir as ir, vmm_opt as opt,
	vmm_parse as parse, vmm_program as program, vmm_tape as tape, vmm_utils as utils,
};
